<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>نظام الدوري المباشر</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root { --primary: #3b82f6; --bg: #0f172a; --card: #1e293b; --text: #f8fafc; --text-dim: #94a3b8; --border: #334155; --gold: #eab308; }
        * { box-sizing: border-box; font-family: 'Tajawal', sans-serif; -webkit-tap-highlight-color: transparent; }
        body { background-color: var(--bg); color: var(--text); margin: 0; padding-bottom: 80px; min-height: 100vh; }
        .app-header { background: var(--card); padding: 15px; display: flex; justify-content: space-between; align-items: center; position: sticky; top: 0; z-index: 100; border-bottom: 1px solid var(--border); }
        .app-header h1 { font-size: 1.1rem; margin: 0; font-weight: 900; color: var(--primary); cursor: pointer; }
        .container { padding: 15px; max-width: 500px; margin: auto; }
        .card { background: var(--card); border-radius: 16px; padding: 18px; margin-bottom: 15px; border: 1px solid var(--border); }
        .card h3 { margin: 0 0 15px 0; font-size: 0.9rem; color: var(--text-dim); border-bottom: 1px solid var(--border); padding-bottom: 8px; }
        table { width: 100%; border-collapse: collapse; }
        th { text-align: right; color: var(--text-dim); font-size: 0.75rem; padding: 8px; }
        td { padding: 12px 8px; border-bottom: 1px solid var(--border); font-size: 0.9rem; }
        .pts-col { color: var(--primary); font-weight: 900; text-align: center; }
        .match-card { display: flex; align-items: center; justify-content: space-between; background: rgba(15, 23, 42, 0.6); padding: 10px; border-radius: 12px; margin-bottom: 10px; border: 1px solid var(--border); }
        .team { width: 32%; text-align: center; font-weight: 700; font-size: 0.9rem; }
        .score-box { display: flex; align-items: center; gap: 8px; }
        .score-input { width: 38px; height: 38px; text-align: center; border: 1px solid var(--border); border-radius: 8px; background: #0f172a; color: white; font-weight: 900; }
        .score-display { font-size: 1.2rem; font-weight: 900; width: 30px; text-align: center; }
        .save-btn { background: #22c55e; color: white; border: none; width: 32px; height: 32px; border-radius: 8px; cursor: pointer; }
        .bottom-nav { position: fixed; bottom: 0; width: 100%; background: var(--card); display: flex; justify-content: space-around; padding: 12px 0; border-top: 1px solid var(--border); z-index: 1000; }
        .nav-item { display: flex; flex-direction: column; align-items: center; color: var(--text-dim); font-size: 0.7rem; width: 33%; cursor: pointer; }
        .nav-item.active { color: var(--primary); font-weight: 900; }
        .page { display: none; }
        .page.active { display: block; }
        .hidden { display: none !important; }
        .banner { padding: 15px; border-radius: 12px; text-align: center; margin-bottom: 15px; font-weight: bold; border: 1px solid; }
        .league-win { background: rgba(59, 130, 246, 0.1); border-color: var(--primary); color: var(--primary); }
        .cup-win { background: rgba(234, 179, 8, 0.1); border-color: var(--gold); color: var(--gold); }
        .btn-next { background: var(--gold); color: #000; border: none; padding: 12px; border-radius: 10px; font-weight: 900; width: 100%; margin-top: 10px; cursor: pointer; }
        #admin-indicator { font-size: 0.6rem; margin-right: 5px; opacity: 0.5; }
    </style>
</head>
<body>

    <div class="app-header">
        <h1 onclick="activateAdmin()">نظام الدوري <span id="admin-indicator">🔒</span></h1>
        <div id="status-tag" style="font-size: 0.7rem; background: #b91c1c; color: white; padding: 4px 10px; border-radius: 20px;">مباشر</div>
    </div>

    <div class="container">
        <div id="page-league" class="page active">
            <div class="card">
                <h3>🏆 ترتيب الدوري</h3>
                <table id="leagueTable">
                    <thead><tr><th>الفريق</th><th>لعب</th><th>+/-</th><th style="text-align:center">نقاط</th></tr></thead>
                    <tbody></tbody>
                </table>
                <div id="leagueWinner" class="banner league-win hidden">بطل الدوري: <span id="l-name"></span> 🛡️</div>
            </div>
            <div class="card"><h3>⚽ المباريات</h3><div id="leagueMatches">جاري التحميل...</div></div>
            <div id="cupSection" class="hidden">
                <div class="card" style="border-top: 4px solid var(--gold);">
                    <h3>🏆 الكأس</h3>
                    <div id="semiMatchArea"></div>
                    <div id="finalPart" class="hidden"><div id="finalMatchArea"></div></div>
                    <div id="cupWinner" class="banner cup-win hidden">بطل الكأس: <span id="c-name"></span> 👑</div>
                </div>
                <button id="nextSeasonBtn" class="btn-next hidden" onclick="startNewSeason()">بدء موسم جديد ➔</button>
            </div>
        </div>
        <div id="page-trophies" class="page"><div class="card"><h3>👑 الأبطال</h3><div id="hallOfFame"></div></div></div>
        <div id="page-archive" class="page"><div class="card"><h3>📂 الأرشيف</h3><div id="archiveList"></div></div></div>
    </div>

    <div class="bottom-nav">
        <div class="nav-item active" onclick="showPage('league', this)"><span>🏟️</span><span>المنافسات</span></div>
        <div class="nav-item" onclick="showPage('trophies', this)"><span>👑</span><span>الأبطال</span></div>
        <div class="nav-item" onclick="showPage('archive', this)"><span>📂</span><span>الأرشيف</span></div>
    </div>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
        import { getDatabase, ref, set, onValue, update } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-database.js";

        const firebaseConfig = {
            apiKey: "AIzaSyA_s2HoKKxwXELNHEmti_RsvA_nP6fmWP8",
            authDomain: "nhffbjhh.firebaseapp.com",
            databaseURL: "https://nhffbjhh-default-rtdb.firebaseio.com",
            projectId: "nhffbjhh",
            storageBucket: "nhffbjhh.firebasestorage.app",
            messagingSenderId: "779073349880",
            appId: "1:779073349880:web:9cd7adb0ed3c628546b30d"
        };

        const app = initializeApp(firebaseConfig);
        const db = getDatabase(app);
        const stateRef = ref(db, 'leagueState');

        // --- إدارة الحالة ---
        let isAdmin = false;
        let state = null;

        window.activateAdmin = () => {
            const p = prompt("كلمة السر للإدارة:");
            if(p === "123") { // غير كلمة السر هنا
                isAdmin = true;
                document.getElementById('admin-indicator').innerText = "🔓 (مسؤول)";
                render();
            }
        };

        // التحميل المباشر من Firebase
        onValue(stateRef, (snapshot) => {
            const data = snapshot.val();
            if (data) {
                state = data;
                render();
                document.getElementById('status-tag').style.background = "#059669";
                document.getElementById('status-tag').innerText = "متصل";
            } else {
                initFirstTime();
            }
        });

        function initFirstTime() {
            const initialState = {
                history: {"أحمد": {l:0, c:0}, "مشعل": {l:0, c:0}, "فيصل": {l:0, c:0}},
                seasons: { 1: createSeasonData(1) },
                curId: 1, viewId: 1
            };
            set(stateRef, initialState);
        }

        function createSeasonData(id) {
            return {
                id, matches: [
                    {h: "أحمد", a: "مشعل", sh: null, sa: null}, {h: "مشعل", a: "فيصل", sh: null, sa: null},
                    {h: "فيصل", a: "أحمد", sh: null, sa: null}, {h: "مشعل", a: "أحمد", sh: null, sa: null},
                    {h: "فيصل", a: "مشعل", sh: null, sa: null}, {h: "أحمد", a: "فيصل", sh: null, sa: null}
                ],
                semi: {h: "", a: "", sh: null, sa: null},
                final: {h: "", a: "", sh: null, sa: null},
                leagueDone: false, cupDone: false, leagueWinner: "", cupWinner: ""
            };
        }

        window.saveMatch = (type, i) => {
            if(!isAdmin) return;
            const h = parseInt(document.getElementById(`${type}h${i}`).value);
            const a = parseInt(document.getElementById(`${type}a${i}`).value);
            if(isNaN(h) || isNaN(a)) return;

            const s = state.seasons[state.viewId];
            if(type === 'L') { s.matches[i].sh = h; s.matches[i].sa = a; }
            if(type === 'S') { s.semi.sh = h; s.semi.sa = a; }
            if(type === 'F') { 
                s.final.sh = h; s.final.sa = a; 
                s.cupWinner = h > a ? s.final.h : s.final.a;
                s.cupDone = true;
                state.history[s.cupWinner].c++;
            }
            update(stateRef, state);
        };

        window.startNewSeason = () => {
            if(!confirm("بدء موسم جديد؟")) return;
            state.curId++;
            state.seasons[state.curId] = createSeasonData(state.curId);
            state.viewId = state.curId;
            update(stateRef, state);
        };

        function render() {
            if(!state) return;
            const s = state.seasons[state.viewId];
            
            // ترتيب الدوري
            let stats = {"أحمد":{p:0,pts:0,gd:0},"مشعل":{p:0,pts:0,gd:0},"فيصل":{p:0,pts:0,gd:0}};
            let played = 0;
            s.matches.forEach(m => {
                if(m.sh !== null) {
                    played++; stats[m.h].p++; stats[m.a].p++;
                    stats[m.h].gd += (m.sh-m.sa); stats[m.a].gd += (m.sa-m.sh);
                    if(m.sh > m.sa) stats[m.h].pts+=3; else if(m.sh < m.sa) stats[m.a].pts+=3; else {stats[m.h].pts++; stats[m.a].pts++;}
                }
            });

            const sorted = Object.keys(stats).sort((a,b) => stats[b].pts-stats[a].pts || stats[b].gd-stats[a].gd);
            document.querySelector("#leagueTable tbody").innerHTML = sorted.map((p, i) => `
                <tr><td><b>${p}</b></td><td>${stats[p].p}</td><td>${stats[p].gd}</td><td class="pts-col">${stats[p].pts}</td></tr>
            `).join("");

            document.getElementById("leagueMatches").innerHTML = s.matches.map((m,i)=>renderMatchRow(m,i,'L',s.leagueDone)).join("");

            if(played === 6) {
                if(!s.leagueDone) { 
                    s.leagueDone = true; s.leagueWinner = sorted[0]; 
                    state.history[s.leagueWinner].l++; 
                    update(stateRef, state);
                }
                document.getElementById("leagueWinner").classList.remove("hidden");
                document.getElementById("l-name").innerText = s.leagueWinner;
                document.getElementById("cupSection").classList.remove("hidden");

                if(!s.semi.h) { s.semi.h = sorted[1]; s.semi.a = sorted[2]; update(stateRef, state); }
                document.getElementById("semiMatchArea").innerHTML = `<h4>الملحق</h4>` + renderMatchRow(s.semi, 0, 'S', s.cupDone);

                if(s.semi.sh !== null) {
                    document.getElementById("finalPart").classList.remove("hidden");
                    const winner = s.semi.sh > s.semi.sa ? s.semi.h : s.semi.a;
                    if(!s.final.h) { s.final.h = s.leagueWinner; s.final.a = winner; update(stateRef, state); }
                    document.getElementById("finalMatchArea").innerHTML = `<h4>النهائي</h4>` + renderMatchRow(s.final, 0, 'F', s.cupDone);
                }
                if(s.cupDone) {
                    document.getElementById("cupWinner").classList.remove("hidden");
                    document.getElementById("c-name").innerText = s.cupWinner;
                    if(isAdmin) document.getElementById("nextSeasonBtn").classList.remove("hidden");
                }
            }
            renderHall(); renderArchive();
        }

        function renderMatchRow(m, i, type, done) {
            const canEdit = isAdmin && !done;
            return `
                <div class="match-card">
                    <span class="team">${m.h}</span>
                    <div class="score-box">
                        ${canEdit ? `<input type="number" id="${type}h${i}" class="score-input" value="${m.sh??''}">` : `<span class="score-display">${m.sh??'-'}</span>`}
                        <span>:</span>
                        ${canEdit ? `<input type="number" id="${type}a${i}" class="score-input" value="${m.sa??''}">` : `<span class="score-display">${m.sa??'-'}</span>`}
                    </div>
                    <span class="team">${m.a}</span>
                    ${canEdit ? `<button class="save-btn" onclick="saveMatch('${type}',${i})">✓</button>` : ''}
                </div>`;
        }

        function renderHall() {
            document.getElementById("hallOfFame").innerHTML = Object.entries(state.history).map(([n, t]) => `
                <div class="card"><b>${n}</b><br><small>الدوري: ${t.l} | الكأس: ${t.c}</small></div>`).join("");
        }

        function renderArchive() {
            document.getElementById("archiveList").innerHTML = Object.keys(state.seasons).map(id => `
                <div onclick="window.viewSeason(${id})" style="cursor:pointer; padding:10px; border-bottom:1px solid #334155">الموسم ${id}</div>`).join("");
        }

        window.viewSeason = (id) => { state.viewId = id; render(); };
        window.showPage = (p, el) => {
            document.querySelectorAll('.page').forEach(pg => pg.classList.remove('active'));
            document.getElementById('page-' + p).classList.add('active');
            document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
            el.classList.add('active');
        };
    </script>
</body>
</html>
