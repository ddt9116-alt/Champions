<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>تطبيق الدوري - الوضع الداكن</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #3b82f6;
            --bg: #0f172a; /* خلفية داكنة جداً */
            --card: #1e293b; /* لون البطاقات */
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --border: #334155;
            --input-bg: #0f172a;
        }

        * { box-sizing: border-box; font-family: 'Tajawal', sans-serif; -webkit-tap-highlight-color: transparent; }

        body {
            background-color: var(--bg);
            color: var(--text-main);
            margin: 0;
            padding-bottom: 80px;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* --- الهيدر العلوي --- */
        .app-header {
            background: var(--card);
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid var(--border);
        }

        .app-header h1 { font-size: 1.2rem; margin: 0; font-weight: 900; color: var(--primary); }

        .container { padding: 15px; width: 100%; max-width: 500px; margin: auto; }

        .card {
            background: var(--card);
            border-radius: 16px;
            padding: 18px;
            margin-bottom: 15px;
            border: 1px solid var(--border);
        }

        .card h3 { margin: 0 0 15px 0; font-size: 0.9rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 1px; }

        /* --- الجدول --- */
        table { width: 100%; border-collapse: collapse; }
        th { text-align: right; color: var(--text-dim); font-size: 0.75rem; padding: 8px; border-bottom: 1px solid var(--border); }
        td { padding: 14px 8px; border-bottom: 1px solid var(--border); font-size: 0.95rem; }
        .pts-col { color: var(--primary); font-weight: 900; text-align: center; font-size: 1.1rem; }

        /* --- المباريات --- */
        .match-card {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: rgba(15, 23, 42, 0.6);
            padding: 12px;
            border-radius: 12px;
            margin-bottom: 10px;
            border: 1px solid var(--border);
        }

        .team { width: 32%; text-align: center; font-weight: 700; font-size: 0.95rem; }
        .score-box { display: flex; align-items: center; gap: 8px; }
        .score-input {
            width: 42px;
            height: 42px;
            text-align: center;
            border: 1px solid var(--border);
            border-radius: 8px;
            font-size: 1.2rem;
            font-weight: 900;
            background: var(--input-bg);
            color: white;
        }

        .save-btn {
            background: var(--primary);
            color: white;
            border: none;
            width: 36px;
            height: 36px;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
        }

        /* --- القائمة السفلية --- */
        .bottom-nav {
            position: fixed;
            bottom: 0;
            width: 100%;
            background: var(--card);
            display: flex;
            justify-content: space-around;
            padding: 12px 0;
            border-top: 1px solid var(--border);
            z-index: 1000;
        }

        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            color: var(--text-dim);
            font-size: 0.7rem;
            width: 25%;
        }

        .nav-item.active { color: var(--primary); font-weight: 900; }
        .nav-icon { font-size: 1.3rem; margin-bottom: 3px; }

        .page { display: none; animation: fadeIn 0.3s ease; }
        .page.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .champion-box {
            background: linear-gradient(135deg, #1e40af, #1e3a8a);
            color: white;
            padding: 30px;
            border-radius: 16px;
            text-align: center;
            margin-top: 10px;
            border: 1px solid var(--primary);
        }

        .btn-next {
            background: #eab308;
            color: #000;
            border: none;
            padding: 14px;
            border-radius: 12px;
            font-weight: 900;
            width: 100%;
            margin-top: 20px;
            font-size: 1rem;
        }

        .hidden { display: none !important; }
    </style>
</head>
<body>

    <div class="app-header">
        <h1>دوري الثلاثة 🏆</h1>
        <div id="status" style="font-size: 0.7rem; background: #064e3b; color: #34d399; padding: 4px 12px; border-radius: 20px; font-weight: bold;">نشط حالياً</div>
    </div>

    <div class="container">
        <div id="page-league" class="page active">
            <div class="card">
                <h3>جدول الترتيب</h3>
                <table id="tableMain">
                    <thead><tr><th>الفريق</th><th>لعب</th><th>+/-</th><th style="text-align:center">نقاط</th></tr></thead>
                    <tbody></tbody>
                </table>
            </div>

            <div class="card">
                <h3>مباريات الدوري</h3>
                <div id="matchesMain"></div>
            </div>

            <div id="playoffs" class="hidden">
                <div class="card" style="border-right: 4px solid #eab308;">
                    <h3>نصف النهائي</h3>
                    <div id="semiArea"></div>
                </div>
                <div class="card" style="border-right: 4px solid var(--primary);">
                    <h3>النهائي الكبير</h3>
                    <div id="finalArea"></div>
                </div>
            </div>

            <div id="winnerAnnounce" class="champion-box hidden">
                <h3 style="color: #cbd5e1; border:none; margin:0">بطل الموسم الحالي</h3>
                <h1 id="champName" style="font-size: 2.5rem; margin: 10px 0;">أحمد</h1>
                <button class="btn-next" onclick="nextSeason()">بدء الموسم التالي ➔</button>
            </div>
        </div>

        <div id="page-trophies" class="page">
            <div class="card">
                <h3>السجل التاريخي للبطولات</h3>
                <div id="trophyList"></div>
            </div>
        </div>

        <div id="page-archive" class="page">
            <div class="card">
                <h3>أرشيف المواسم السابقة</h3>
                <div id="archiveList"></div>
            </div>
        </div>
    </div>

    <div class="bottom-nav">
        <div class="nav-item active" onclick="showPage('league', this)">
            <span class="nav-icon">⚽</span>
            <span>الدوري</span>
        </div>
        <div class="nav-item" onclick="showPage('trophies', this)">
            <span class="nav-icon">👑</span>
            <span>الأبطال</span>
        </div>
        <div class="nav-item" onclick="showPage('archive', this)">
            <span class="nav-icon">📂</span>
            <span>الأرشيف</span>
        </div>
    </div>

<script>
    let db = {
        trophies: {"أحمد": 0, "مشعل": 0, "فيصل": 0},
        seasons: { 1: createSeason(1) },
        curId: 1, viewId: 1
    };

    function createSeason(id) {
        return {
            id, matches: [
                {h: "أحمد", a: "مشعل", sh: null, sa: null}, {h: "مشعل", a: "فيصل", sh: null, sa: null},
                {h: "فيصل", a: "أحمد", sh: null, sa: null}, {h: "مشعل", a: "أحمد", sh: null, sa: null},
                {h: "فيصل", a: "مشعل", sh: null, sa: null}, {h: "أحمد", a: "فيصل", sh: null, sa: null}
            ],
            semi: {h: "", a: "", sh: null, sa: null},
            final: {h: "", a: "", sh: null, sa: null},
            done: false, winner: ""
        };
    }

    function showPage(pageId, el) {
        document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
        document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
        document.getElementById('page-' + pageId).classList.add('active');
        el.classList.add('active');
        if(pageId === 'trophies') renderTrophies();
        if(pageId === 'archive') renderArchive();
    }

    function renderTrophies() {
        document.getElementById("trophyList").innerHTML = Object.entries(db.trophies)
            .sort((a,b)=>b[1]-a[1]).map(([n,c])=>`
            <div style="display:flex; justify-content:space-between; padding:18px; background:rgba(255,255,255,0.03); margin-bottom:10px; border-radius:12px; border:1px solid var(--border);">
                <span style="font-weight:bold; font-size:1.1rem">${n}</span>
                <span style="color:#eab308; font-weight:900; font-size:1.2rem">${c} 🏆</span>
            </div>
        `).join("");
    }

    function renderArchive() {
        document.getElementById("archiveList").innerHTML = Object.keys(db.seasons).sort((a,b)=>b-a).map(sid=>`
            <div onclick="switchSeason(${sid})" style="padding:18px; background:${sid==db.viewId?'#3b82f6':'rgba(255,255,255,0.03)'}; color:${sid==db.viewId?'white':'var(--text-main)'}; margin-bottom:10px; border-radius:12px; display:flex; justify-content:space-between; border:1px solid ${sid==db.viewId?'#3b82f6':'var(--border)'};">
                <span>الموسم ${sid}</span>
                <span>${db.seasons[sid].done?'🏆 منتهي':'⏳ نشط'}</span>
            </div>
        `).join("");
    }

    function switchSeason(sid) {
        db.viewId = sid;
        showPage('league', document.querySelector('.nav-item'));
        renderLeague();
    }

    function renderLeague() {
        const s = db.seasons[db.viewId];
        document.getElementById("status").innerText = s.done ? "موسم منتهي" : "موسم نشط";
        
        let stats = {"أحمد":{p:0,pts:0,gd:0},"مشعل":{p:0,pts:0,gd:0},"فيصل":{p:0,pts:0,gd:0}};
        let played = 0;
        s.matches.forEach(m => {
            if(m.sh !== null) {
                played++; stats[m.h].p++; stats[m.a].p++;
                stats[m.h].gd += (m.sh-m.sa); stats[m.a].gd += (m.sa-m.sh);
                if(m.sh > m.sa) stats[m.h].pts+=3; else if(m.sh < m.sa) stats[m.a].pts+=3; else {stats[m.h].pts++; stats[m.a].pts++;}
            }
        });

        const sorted = ["أحمد","مشعل","فيصل"].sort((a,b) => stats[b].pts-stats[a].pts || stats[b].gd-stats[a].gd);
        document.querySelector("#tableMain tbody").innerHTML = sorted.map(p => `
            <tr><td>${p}</td><td>${stats[p].p}</td><td>${stats[p].gd > 0 ? '+'+stats[p].gd : stats[p].gd}</td><td class="pts-col">${stats[p].pts}</td></tr>
        `).join("");

        document.getElementById("matchesMain").innerHTML = s.matches.map((m,i)=>createMatchRow(m,i,'L',s.done)).join("");

        const pArea = document.getElementById("playoffs");
        const wArea = document.getElementById("winnerAnnounce");

        if(played === 6) {
            pArea.classList.remove("hidden");
            if(!s.semi.h) { s.semi.h = sorted[1]; s.semi.a = sorted[2]; }
            document.getElementById("semiArea").innerHTML = createMatchRow(s.semi,0,'S',s.done);
            if(s.semi.sh !== null) {
                const swin = s.semi.sh > s.semi.sa ? s.semi.h : s.semi.a;
                if(!s.final.h) { s.final.h = sorted[0]; s.final.a = swin; }
                document.getElementById("finalArea").innerHTML = createMatchRow(s.final,0,'F',s.done);
                if(s.done) {
                    wArea.classList.remove("hidden");
                    document.getElementById("champName").innerText = s.winner;
                } else { wArea.classList.add("hidden"); }
            }
        } else { pArea.classList.add("hidden"); wArea.classList.add("hidden"); }
    }

    function createMatchRow(m, i, type, dis) {
        return `
            <div class="match-card">
                <span class="team">${m.h}</span>
                <div class="score-box">
                    <input type="number" id="${type}h${i}" class="score-input" value="${m.sh??''}" ${dis?'disabled':''}>
                    <span style="color:var(--text-dim)">:</span>
                    <input type="number" id="${type}a${i}" class="score-input" value="${m.sa??''}" ${dis?'disabled':''}>
                </div>
                <span class="team">${m.a}</span>
                ${!dis ? `<button class="save-btn" onclick="save('${type}',${i})">✓</button>` : ''}
            </div>
        `;
    }

    function save(type, i) {
        const s = db.seasons[db.viewId];
        const h = document.getElementById(type+'h'+i).value;
        const a = document.getElementById(type+'a'+i).value;
        if(h==="" || a==="") return;
        if(type==='L') { s.matches[i].sh=parseInt(h); s.matches[i].sa=parseInt(a); }
        else if(type==='S') { s.semi.sh=parseInt(h); s.semi.sa=parseInt(a); }
        else if(type==='F') { 
            s.final.sh=parseInt(h); s.final.sa=parseInt(a); 
            s.winner = s.final.sh > s.final.sa ? s.final.h : s.final.a;
            s.done = true; db.trophies[s.winner]++;
        }
        renderLeague();
    }

    function nextSeason() {
        db.curId++;
        db.seasons[db.curId] = createSeason(db.curId);
        db.viewId = db.curId;
        renderLeague();
    }

    renderLeague();
</script>
</body>
</html>
